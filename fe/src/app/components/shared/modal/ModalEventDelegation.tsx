'use client';

import { useModalEventDelegation } from './useModalEventDelegation';

/**
 * modal-id 속성을 가진 버튼 클릭 이벤트를 처리하는 컴포넌트
 */
export default function ModalEventDelegation() {
  useModalEventDelegation();
  return null;
}
